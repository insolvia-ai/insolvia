[project]
name = "insolvia-mailer"
version = "0.1.0"
description = "Insolvia transactional email service — SigV4 ingress, S3 manifests, SQS, SES sender (mailer port)"
requires-python = ">=3.12"
# Runtime dependencies are pinned in requirements.txt, mirroring services/api;
# the Docker image and CI install from there rather than from this metadata.
dependencies = []

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
addopts = "-q"

# Strict static type checking (mirrors the CI `mypy` step). Scoped to `src`;
# tests exercise fakes/monkeypatching where --strict is noise, not signal.
[tool.mypy]
python_version = "3.12"
strict = true
mypy_path = "src"
files = ["src"]

[[tool.mypy.overrides]]
# Third-party packages that ship no type information.
module = ["boto3.*", "botocore.*", "mangum.*"]
ignore_missing_imports = true
