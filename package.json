{
  "name": "insolvia-workspace",
  "private": true,
  "type": "module",
  "//": [
    "Root of the Insolvia npm workspace. NOT a shippable package — it declares",
    "the members and pins the shared toolchain.",
    "",
    "The member list is EXPLICIT and must stay that way. `apps/insolvia_marketing`",
    "is deliberately NOT a member: it has its own lockfile and is packaged into a",
    "standalone Docker image with `npm ci --omit=dev`, so making it a workspace",
    "member (symlinking its deps from this root) would let it build here against",
    "packages the image will not contain. `packages/*` globs are likewise banned",
    "— a new package is added by name once, on purpose.",
    "",
    "CAUTION — Node resolution walks UP the tree. A missing dependency in",
    "apps/insolvia_marketing can resolve from this root's node_modules and pass a",
    "local build, then fail in the clean `npm ci` its own CI job and Docker build",
    "run. That separate lockfile + separate job is what catches it — do not",
    "consolidate marketing into this workspace."
  ],
  "engines": {
    "node": ">=24"
  },
  "workspaces": [
    "apps/insolvia_app",
    "packages/insolvia_tokens",
    "packages/insolvia_api_client"
  ],
  "scripts": {
    "//tokens": [
      "Run with plain `node`, no loader and no build step: Node >=24 strips",
      "TypeScript types natively, so the generator stays dependency-free.",
      "Type-stripping cannot execute `enum`, `namespace`, or parameter",
      "properties — the generator must avoid all three (erasableSyntaxOnly in",
      "tsconfig.base.json enforces it)."
    ],
    "tokens": "node packages/insolvia_tokens/tool/generate-tokens.ts",
    "tokens:check": "node packages/insolvia_tokens/tool/generate-tokens.ts --check",
    "//lint": [
      "Fans out per member rather than running ESLint once from the root, and",
      "there is deliberately NO `eslint.config.js` at the repo root — the rules",
      "live in `eslint.base.js`, which each member re-exports. ESLint discovers",
      "a flat config by walking UP from its cwd, so a root one would reach into",
      "subdirectories that answer to their own config. Read eslint.base.js first."
    ],
    "lint": "npm run lint --workspaces --if-present",
    "format": "prettier --check apps/insolvia_app packages/insolvia_tokens packages/insolvia_api_client",
    "format:write": "prettier --write apps/insolvia_app packages/insolvia_tokens packages/insolvia_api_client",
    "//app": [
      "The app's own scripts (`build`, `web`, `lint`, `typecheck`, `test`) live in",
      "apps/insolvia_app/package.json and are a contract with app-*.yml. These two",
      "are conveniences for a developer at the repo root: `app` starts the dev",
      "server on port 3000 (the exact-match Cognito origin), `app:build` produces",
      "the production web export."
    ],
    "app": "npm run web --workspace @insolvia-ai/app",
    "app:build": "npm run build --workspace @insolvia-ai/app",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "//ci": [
      "The whole local gate: token drift, format, lint, typecheck, test. Mirrors",
      "what the CI workflows run per area, so `npm run ci` green locally is a",
      "fair predictor of a green PR."
    ],
    "ci": "npm run tokens:check && npm run format && npm run lint && npm run typecheck && npm run test"
  },
  "//devDependencies": [
    "ESLint/Prettier versions match apps/insolvia_marketing deliberately — the",
    "app added in PR 3 is told to match marketing's lint setup, so a second",
    "ESLint major in the repo would be a trap, not a choice.",
    "TypeScript is 5.9 rather than the 7.x line: `erasableSyntaxOnly` (5.8+) is",
    "the feature this workspace depends on, and adopting a rewritten compiler in",
    "the middle of a toolchain migration would confound any failure."
  ],
  "devDependencies": {
    "@eslint/js": "^9.17.0",
    "@types/node": "^24.13.3",
    "eslint": "^9.17.0",
    "globals": "^15.14.0",
    "prettier": "^3.3.3",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.19.0",
    "vitest": "^4.1.10"
  }
}
