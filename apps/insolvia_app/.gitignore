# Expo / Metro
.expo/
dist/
web-build/
expo-env.d.ts
.metro-health-check*

# Native projects are GENERATED, never committed: `expo prebuild` recreates
# ios/ and android/ from app.config.ts if and when mobile starts
# (docs/adr/0004-react-native-replaces-flutter.md).
/ios
/android
