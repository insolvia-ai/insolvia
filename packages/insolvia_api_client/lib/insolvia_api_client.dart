/// A hand-written, typed Dart client for the Insolvia API (`services/api`).
///
/// Pure Dart — usable from the Flutter app, scripts, or any other Dart
/// context. The JSON contract it encodes is pinned by this package's tests;
/// see README.md for why there is no OpenAPI codegen (yet).
library;

export 'src/client.dart';
export 'src/exceptions.dart';
export 'src/models.dart';
