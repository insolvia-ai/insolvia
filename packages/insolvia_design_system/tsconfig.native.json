{
  "//": [
    "The NATIVE half of the platform-split typecheck (`npm run",
    "typecheck:native`). Same trick as tsconfig.json — read its comment — with",
    "the suffix list flipped to ['.native', ''] so './button' resolves to",
    "button.native.tsx, exactly as Metro does. No DOM lib: the native leaves",
    "must typecheck against React Native's own types (shipped with",
    "react-native itself, a devDependency here), not against a browser",
    "environment they never see. The .web.tsx leaves and the (web) tests are",
    "the other program."
  ],
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "moduleSuffixes": [".native", ""],
    "jsx": "react-jsx",
    "lib": ["ES2023"],
    "types": []
  },
  "include": ["src"],
  "exclude": ["src/**/*.web.tsx", "src/**/*.test.ts", "src/**/*.test.tsx", "src/styles"]
}
